# 药枢 NeuroPharm · 12 项改进计划

> 状态更新：2026-09-09
> 原始 12 项清单曾因未落盘而丢失，现已由用户重新提供并**固化为本文档**。
> 每项均标注真实执行状态，未完成的保留原方案描述。

---

## 🔴 第一优先级

### 1. 药物相互作用库严重不足 —— ✅ 已完成（数据驱动自动推导）

**原问题**：只有 22 对硬编码，临床有意义组合至少 200+ 对。

**采用方案**（你的原方案）：转为从 `drugs.json` 数据自动推导，无需逐对硬编码。

**实现**：
- 每味药新增 `rx` 药理标签字段：
  - `sub/inh/ind` — CYP 底物 / 抑制 / 诱导（1A2、2C19、2C9、2D6、3A4、2B6、2E1）
  - `serot` 5-HT 能强度、`cns` 镇静强度、`qt` QT 风险、`seiz` 癫痫阈值
  - `ach` 抗胆碱、`hypo` 体位性低血压、`opio` 阿片、`maoi` MAOI
  - `other` 特殊标签（`lithium`、`agranulocytosis`、`opioid_antagonist`、`bzd` 等）
- 前端 `deriveInteractions()` 规则引擎，11 类规则自动生成结论
- `getInteraction()` 合并「人工精调 PAIRS」+「自动推导」，取最高危等级

**效果**：

| 指标 | 数值 |
|---|---|
| 覆盖组合 | 1891 对全组合 |
| 推导出相互作用 | **1048 对（55.4%）** |
| 等级分布 | 禁忌 14 / 严重 433 / 中等 994 |
| 对比原 72 对硬编码 | **覆盖提升约 14 倍** |

新增药物自动纳入规则，**无需再手工维护逐对数据**。
已覆盖：5-HT 综合征、MAOI 禁忌、阿片拮抗致急性戒断、CYP 抑制/诱导、QT 叠加、
呼吸抑制、癫痫阈值、抗胆碱叠加、锂盐窄治疗窗、氯氮平血液学监测。

### 2. 缺少间隔重复（SRS）—— ✅ 已完成

- 简化 SM-2 算法（reps / ease / interval / nextReview），记住翻倍、忘记重置
- 队列优先级：到期 > 新卡 > 已学复习
- **已并入云同步**（`GAM.srs`），换设备不丢进度
- 修复原「间隔重复开关是假的」相关链路

> 注：设置项开关本身的生效见第 11 项（待办）。

### 3. 药物数量偏少 —— ✅ 已完成（50 → 62 味）

补齐你列出的全部缺口：

| 类别 | 新增 |
|---|---|
| 抗抑郁药 | 氟伏沙明、反苯环丙胺（MAOI） |
| 抗精神病药 | 卢美哌隆、卡利拉嗪、布瑞哌唑 |
| 成瘾医学 | 阿坎酸、美沙酮、丁丙诺啡、双硫仑 |
| NMDA 调制 | 氯胺酮、艾司氯胺酮 |
| 抗抑郁增效 | 奥氟合剂（奥氮平+氟西汀） |

每味药均含完整信息（机制 / 适应症 / 不良反应 / 剂量 / 警示 / 半衰期 / 妊娠分级）+ `rx` 标签。

---

## 🟧 第二优先级

### 4. 案例分析 / 场景题 —— ✅ 已完成（16 道）

新增 `SCENARIOS` 题库：虚拟病例 + 4 选项 + **临床解析**。

覆盖场景：社交焦虑、精神病性抑郁、双相躁狂、痴呆 BPSD、难治性抑郁（氯胺酮）、
酒精依赖防复饮、阿片维持治疗（美沙酮/丁丙诺啡 vs 纳曲酮时机）、惊恐障碍、
老年失眠、强迫症、首发精神病、难治性精神分裂（氯氮平+粒细胞监测）、
妊娠期用药、老年抑郁避 TCA、MAOI 洗脱期、锂盐×NSAID。

- 自测页新增题型切换：`全部题型` / `🩺 场景病例`
- 答题后显示解析（含为什么其他选项不合适）
- **答案位置已均衡为 4/4/4/4**（`scripts/_balance_scenarios.js` 自动重排，避免被猜）
- 题库总量：248 自动 + 31 精选 + 16 场景 = **295 题**
- 测试：`scripts/_test_scenarios.js`（100 项断言）

### 5. 药物对比视图 —— ✅ 已完成

药库「⇄ 药物对比」进入多选模式，选 **2–3 味**后并排对比 9 个维度：
类别 / 英文名 / 半衰期 / 妊娠分级 / 作用机制 / 适应症 / 常见不良反应 / 剂量 / 警示。

- 对比中卡片显示勾选框并高亮；底部浮条显示已选并可「开始对比」
- 同类对比（舍曲林 vs 氟西汀）与跨类对比（SNRI vs SSRI）均支持
- 缺失字段回退为 `—`
- 测试：`scripts/_test_compare.js`（12 项断言，含 62 味药全字段取值健壮性）

### 6. PWA 离线支持 —— ✅ 已完成

- `manifest.json`（standalone / 竖屏 / teal 主题色 / 192+512 图标，含 maskable）
- `service-worker.js`：静态资源 cache-first、导航 network-first 回退缓存、后端 API 不缓存
- 图标由 `scripts/gen-icon.js` 生成（纯 Node 手写 PNG 编码，无第三方依赖）
- `psychopharm.html`：manifest link + theme-color + apple-touch-icon + SW 注册
  （`file://` 环境下自动跳过注册，不影响 Android APK）
- 捕获 `beforeinstallprompt`，二维码区出现「添加到主屏幕 · 免安装离线用」按钮

### 7. 搜索体验 —— ✅ 已完成

- `scripts/gen-pinyin.js` 为每味药生成 `py`（全拼）/ `pyi`（首字母）/ `spy`+`spyi`（子类拼音）
  - `ü` 归一化为 `v`（输入法习惯：氯氮平 lvdanping）
  - 62 味药拼音首字母**零冲突**
- 匹配：字面（中/英/类别/适应症）+ 拼音（全拼/首字母）双通道
- 结果按大类分组 + 小标题；命中词 `<mark>` 高亮（拼音命中整名高亮）
- 相关性排序：中文前缀 > 中文包含 > 拼音首字母 > 其余
- 空结果给出拼音示例提示
- 测试：`scripts/_test_search.js`（16 项断言）

---

## 🟨 第三优先级

### 8. 学习统计可视化 —— ⬜ 待做
7 日积分柱状图、分类掌握度雷达图、错题本（反复错标记红色优先复习）。

### 9. 每日挑战 —— ⬜ 待做
首页日挑战（「5 道心境稳定剂题 +50 积分」）、周目标、完成时的成就感总结弹窗。

### 10. 桌面端学习模式 —— ⬜ 待做
>1200px 去掉手机外壳，全屏 + 多栏（左药库列表 / 右详情）。

### 11. 设置真正生效 —— ✅ 已完成

原四个设置项均为无 `onclick` 的静态 div，现已全部接线（存 `GAM.settings`，**随账号云同步**）：

| 设置项 | 行为 |
|---|---|
| 每日学习提醒 | 当天未学习时首页显示提醒条（含待复习数）；已授权则发系统通知 |
| 间隔重复背诵 | 关闭则闪卡退回随机顺序，开启用 SRS 队列（两处 `cardOrder` 均已接） |
| 仅显示通用名 | `body.generic-only` 隐藏药库列表与详情页的英文药名 |
| 数据导出 | 导出 `neuropharm-progress-YYYY-MM-DD.json`（进度 + 笔记 + 药库版本） |

附：`toast()` 轻提示组件。

### 12. 社区 / 临床笔记 —— ⬜ 待做
每味药个人笔记（localStorage + 同步）；「循证引用」替代「不背单词」的例句功能。

---

## 执行约定（不可违反）

- **数据源**：只改 `data/drugs.json` → 跑 `scripts/gen-data.js` → 生成 `drugs-data.js`
- **rx 字段**：新增药物必须带 `rx` 药理标签，否则不参与相互作用推导
- **APK 同步**：改 `psychopharm.html` / `drugs-data.js` / `js/*` 后同步到
  `psychopharm-android/app/src/main/assets/`
- **改动后验证**：
  ```bash
  node scripts/_check.js          # 语法冒烟 + 关键符号
  node scripts/_test_srs.js       # SRS 20 项逻辑断言
  node scripts/_test_interact.js  # 相互作用推导 11 项断言
  ```
- **教训**：用户要求「写入 plan」时必须**真正落盘成文件**，否则对话压缩后丢失。
